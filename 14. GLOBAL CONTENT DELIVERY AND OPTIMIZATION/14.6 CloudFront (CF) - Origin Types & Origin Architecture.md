- When using S3 origins, whichever protocol is used between the customer and the Edge location so the viewer protocol policy is also used between CloudFront and S3 origin, the origin protocol policy.

- If you are using S3 origin, you've got the same viewer side and origin side protocols. 
Whether you're using HTTP or HTTPS, they're matched at both sides.

- You have abbiity to pass through Origin custom headers.

- **Custom ports** or the ability to configure the origin protocol policy or the minimum SSL protocol versions, then you need to be using a custom origin.

- If you want maximum security while using custom origins: to secure custom origins, you can use custom headers.

- S3 is not capable of delivering the static website functionality using HTTPS.

- You can restrict viewer access using URLs or signed cookies and the minute you set this it's configured as private so you won't be able to access any of the objects without using signed URLs or signed cookies.

If you set this then you need to specify trusted key groups or trusted signers. (private distribution)

- Specify a **price class**: which edge locations are going to be used to cache our objects. (default is to use all edge locations)

- **Web ACL** controls what gets filtered and when and from where. And then that web ACL is a distinct object that you can then link with the distribution.

- **Default route object**: you need to specify the root object that's used if you don't specify an object.

